{
  "title": "JSON schema for Oradew dbconfig file",
  "$schema": "http://json-schema.org/draft-06/schema#",
  "type": "object",
  "properties": {
    "DEV": {
      "type": "object",
      "$ref": "#/definitions/ConnectionConfig",
      "default": {
        "connectString": "localhost/orclpdb",
        "users": [
          {
            "user": "hr",
            "password": "welcome"
          },
          {
            "user": "hr1",
            "password": "welcome1",
            "default": true
          }
        ]
      }
    },
    "TEST": {
      "type": "object",
      "$ref": "#/definitions/ConnectionConfig",
      "default": {
        "connectString": "localhost/orclpdb",
        "users": [
          {
            "user": "hr",
            "password": "welcome"
          }
        ]
      }
    },
    "UAT": {
      "type": "object",
      "$ref": "#/definitions/ConnectionConfig",
      "default": {
        "connectString": "localhost/orclpdb",
        "users": [
          {
            "user": "hr",
            "password": "welcome"
          }
        ]
      }
    }
  },
  "definitions": {
    "ConnectionConfig": {
      "type": "object",
      "properties": {
        "connectString": {
          "type": "string"
        },
        "users": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "user": {
                "type": "string"
              },
              "password": {
                "type": "string"
              },
              "schema": {
                "description": "Principal schema for user. \nIf \"user\" doesn't own any object, schema must be specified.",
                "type": "string"
              },
              "default": {
                "description": "Default user for environment. \nWhenever user can't be extracted from path, default user (\"default\": true) will be used for connection. (ex. for deploy)",
                "type": "boolean"
              }
            },
            "required": ["user", "password"],
            "default": {
              "user": "hr",
              "password": "welcome"
            }
          }
        }
      },
      "required": ["connectString", "users"]
    }
  }
}
